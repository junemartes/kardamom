# Managed by Ansible (roles/consul): the local Consul agent resolves
# every name and forwards names outside .consul to the recursors.
# consul-resolver.service copies this file over /etc/resolv.conf at
# every boot.
nameserver 127.0.0.1
