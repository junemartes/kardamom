# 22 239e632 — refactor(log): remove dead/legacy code and tech debt (#43)

## Summary of change
Removal-only cleanup of `crates/log/` after the move to archive-at-the-sealer durability: deletes the legacy Q-of-N / per-recorder-fsync machinery (`run_watermark_loop`, `Recorder::start_b`/`start_a`, `WatermarkPublisher`, `WatermarkAPublisher`, `LogError::QuorumStalled`, `RecorderKind::TxData`), the unused raw publisher structs (`TxReceiptsPublisher`, `TxErrorsPublisher`, `TxDepositsPublisher`, `Publishers` bundle), unused `Recorder` accessors, the whole superseded `tx_ordering_archive.rs` module, and the struct/descriptor fields that fell out. Also moves `alloy-primitives`/`bytes` to dev-dependencies and trims stale doc comments.

## Findings

Dead-code verification: every removed pub item was grep-checked at the commit tree and at HEAD — `run_watermark_loop`, `start_b`, `start_a`, `WatermarkPublisher`, `WatermarkAPublisher`, `QuorumStalled`, `RecorderKind::TxData`, `TxReceiptsPublisher`, `TxErrorsPublisher`, `TxDepositsPublisher`, `Publishers`, `recorder_id()`, `kind()`, `archive()`, `active_segment_path()`, `tx_ordering_archive` (incl. `ArchiveMessage`, `open_replay_subscription`, `SegmentFileReader`). Zero code references outside the deleted definitions in either tree; the live path uses the `*Handle` variants in `aeron_live.rs` and `run_durable_watermark_loop`. The dep move is sound: `alloy_primitives` in `crates/log/src/codec.rs` is used only inside `#[cfg(test)] mod tests` (line 54), so dev-dependency scope is correct. No tests, error handling, or metrics were removed — only definitions and their doc text. `FsyncWatermark` the *type* (kardamom-types) is still legitimately used by ingress/aeron_live, so removing only the log-crate publisher of it is consistent.

### F22.1 [nit] [quality] — Doc comments still reference the removed `kardamom_log::TxDepositsPublisher`
- **Where**: crates/da_watcher/src/lib.rs:21 and crates/da_watcher/src/publisher.rs:3 (same lines at HEAD)
- **What**: The commit removed `TxDepositsPublisher` but left two da_watcher doc comments saying the live adapter is "`kardamom_log::TxDepositsPublisher`"; the real adapter wraps `TxDepositsPublisherHandle`. The later da_watcher cleanup (62c65b6) did not fix these either. Purely docs drift; misleading to readers, no behavioral impact.
- **Still present at HEAD**: yes
- **Suggested fix**: Update both comments to name `kardamom_log::TxDepositsPublisherHandle`.

### F22.2 [nit] [quality] — Historical spec doc still describes removed items (informational)
- **Where**: docs/agents/log-config-and-recorder-spec.md:14,68,97,99 (same at HEAD)
- **What**: The dated design spec (2026-06-12, "Status: Draft") still names `run_watermark_loop`, `WatermarkPublisher`, `Recorder::start_b()/start_a()`. As a point-in-time spec this is arguably intentional historical record, but nothing marks it as superseded by the archive-at-the-sealer design.
- **Still present at HEAD**: yes
- **Suggested fix**: Optionally add a one-line "superseded by archive-at-the-sealer durability" note at the top; no code change needed.

## Verdict
Clean, well-executed removal-only commit. Everything deleted was verifiably dead at the commit and remains unreferenced at HEAD; the dependency reclassification is correct; kept-for-compat parameters in `start_b_mdc` are explicitly documented (`let _ = (recorder_id, archive_dir)`). Only residue is two stale doc-comment references in da_watcher and the historical spec doc.
