# 25 e561fc6 — refactor(sequencer): remove dead/legacy code and tech debt (#52)

## Summary of change
Dead-code cleanup of `kardamom-sequencer`: removes the standby-side `PartitionState::replay()` (plus its two unit tests) left over from the abandoned primary/standby design, the never-constructed `SequencerError::Log` variant and its `From<LogError>` impl, the uncalled `iter_next_nonces()` and `with_sequencer_id_from_partition()` helpers (plus the latter's self-test), the never-configured `BackpressurePolicy::SpinRetry` variant, and unused `ctrlc`/`rkyv` dependencies. Also fixes the `reinsert_for_retry` doc that referenced the removed dual-write design.

## Findings

Dead-code verification: grep at the commit tree and at HEAD found zero references to `PartitionState::replay`, `iter_next_nonces`, `SpinRetry`/`spin_retry` (including deploy templates and e2e TOML), `with_sequencer_id_from_partition`, `SequencerError::Log`, or `ctrlc`; `rkyv` had no use in sequencer sources. The three deleted tests only exercised deleted code, so no live-path coverage was lost. The judgment call to keep `BackpressurePolicy`/`ReturnImmediately` because config TOMLs carry the field under `deny_unknown_fields` is correct and correctly explained. The updated doc reference `crate::sequencer::Sequencer::run_once` resolves at HEAD (crates/sequencer/src/sequencer.rs:216), so the doc fix is accurate, and the removal of the standby/dual-write narrative matches the current single-B-write design. Notably, the sequencer replication work shipped later (PR #75) did not reintroduce a standby nonce-replay path, so removing `replay()` did not force a reimplementation.

No findings — the commit is clean.

## Verdict
Clean removal-only commit with accurate reasoning. All removed items were dead at the commit and remain unreferenced at HEAD; error-variant and dependency pruning check out; the doc corrections fix genuine drift rather than introducing any. Nothing of functional value (tests, error handling, metrics) was lost.
