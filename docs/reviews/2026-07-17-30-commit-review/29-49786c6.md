# 29 49786c6 — refactor(sealer): remove dead/legacy code and tech debt (#48)

## Summary of change
Small, conservative cleanup of `kardamom-sealer`: removes the unused `pub fn next_after` helper in bootstrap.rs (the production bootstrap inlines the identical `max.map_or(1, |n| n + 1)` logic) together with its self-referential `next_after_helpers_agree` test, drops the unreferenced `rkyv` dependency, and fixes a stale doc link (`max_block_number_from_iter` → the real `next_block_number_from_iter`).

## Findings

Dead-code verification: `next_after` had no references at the commit tree or anywhere afterwards; the deleted test asserted the same three cases the surviving `next_block_number_from_iter` tests cover via `map_or(1, |n| n + 1)`, so no meaningful coverage was lost. `rkyv` has zero source references in the sealer crate at the commit — the wire types carry their derives in kardamom-types. The doc fix points at a helper that exists. The commit's "kept" list (`SealerConfig` fields backed by the deployed TOML template under `deny_unknown_fields`, `current_tx_tail`, `observe_boundary`/`tick_once`, clock types) was accurate at the commit. Note that the entire `crates/sealer` crate was later deleted wholesale by 471d39d (active/active ingress + Aeron Cluster sealer, #67), so nothing from this commit survives to HEAD in any form — there is no possibility of dangling references today.

No findings — the commit is clean.

## Verdict
Clean, minimal removal commit: one trivially duplicated helper, one unused dependency, one doc-link fix. Everything removed was dead at the commit, the removed test duplicated retained coverage, and the whole crate was subsequently replaced by the Aeron Cluster sealer anyway, making the change doubly moot at HEAD.
