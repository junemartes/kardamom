# 28 62c65b6 — refactor(da-watcher): remove dead/legacy code and tech debt (#44)

## Summary of change
Dead-code cleanup of `crates/da_watcher`: removes the unused `derive::DEPOSIT_TX_TYPE` const (a port artifact — `crates/node` keeps its own independent copy, which is the one actually referenced), drops the never-enabled `testing` feature and narrows the `fakes` module gates from `#[cfg(any(test, feature = "testing"))]` to `#[cfg(test)]`, and prunes unused dependencies (`humantime-serde`, `serde`, `toml`, and the redundant dev-dep `alloy-primitives`).

## Findings

Dead-code verification: every `DEPOSIT_TX_TYPE` reference at the commit and at HEAD resolves to `crates/node/src/deposit.rs`'s own copy — the da_watcher const was unreferenced. No crate ever enabled `features = ["testing"]` on da_watcher, and the `fakes` modules are consumed only by this crate's own inline `#[cfg(test)]` tests (publisher.rs:66, watcher.rs:250-251 at HEAD), so the gate narrowing changes nothing observable. `serde`/`toml`/`humantime` have zero source references in da_watcher src/tests at the commit; the binary's config path goes through `kardamom_log::config::LogConfig::resolve`. The deliberate keeps (`PublishError::Closed`/`MonitorError::PublisherClosed` as a documented behavior-model, `alias_l1_address`) are reasonable and still hold at HEAD. No tests, error handling, or metrics were removed.

### F28.1 [nit] [quality] — Missed chance to fix the adjacent stale doc reference
- **Where**: crates/da_watcher/src/publisher.rs:3 and crates/da_watcher/src/lib.rs:21 (same at HEAD)
- **What**: This da_watcher-focused cleanup edited publisher.rs (the cfg gate on line 31) but left line 3's doc comment pointing at `kardamom_log::TxDepositsPublisher`, a type deleted by the sibling log cleanup (239e632) in the same PR series; the real adapter wraps `TxDepositsPublisherHandle`. Cross-referenced as F22.1 — recorded there as the primary finding; noted here because this commit was the natural place to fix it.
- **Still present at HEAD**: yes
- **Suggested fix**: Point both doc comments at `kardamom_log::TxDepositsPublisherHandle`.

## Verdict
Clean and conservative removal commit. The removed const was verifiably a duplicate-port artifact, the feature-flag removal is behavior-identical (fakes are strictly intra-crate test code), and dependency pruning matches actual usage. Only residue is the stale doc reference to the log crate's removed publisher type, which this commit could have fixed in passing.
