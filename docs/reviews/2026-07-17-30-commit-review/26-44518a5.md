# 26 44518a5 — refactor(executor): remove dead/legacy code and tech debt (#51)

## Summary of change
Dead-code cleanup of `kardamom-executor`: removes the never-constructed `ExecutorError::StateWriterClosed`, the `apply_write_set` convenience wrapper (its one test call site rewired to `delta.apply(..)`, preserving merge coverage), `WriteSet::is_empty`, the unused `WriterApplyingQueue::take_log` and its backing `log` field, `DepositJoinBuffer::{len,is_empty}`, a stale dev note in executor.rs, stale doc references to a nonexistent `crate::testing::Vec*Sub` module, and the unused `alloy-rlp` dependency.

## Findings

Dead-code verification: grep at the commit tree and at HEAD found zero references to `StateWriterClosed`, `apply_write_set` (the free fn — only a similarly named test, `apply_write_set_merges_and_overwrites`, survives in the successor `crates/engine/src/delta.rs` and calls `delta.apply` directly), `WriteSet::is_empty`, `take_log`, or `DepositJoinBuffer::{len,is_empty}`. `alloy_rlp` has no use in the executor crate (nor in its successor `crates/engine`) at either tree; other crates that use it declare it themselves. The test for merge behaviour was preserved by inlining `delta.apply`, so no coverage was lost. `WriterApplyingQueue`'s removed `log` was a test-inspection hook with no consumers (`take_log` never called), so dropping the `Arc<RwLock<Vec<..>>>` bookkeeping from `submit` loses nothing and slims the mock. The kept/removed judgment calls (`JoinBuffer::len` kept for the growth-monitor warn, `is_empty` kept for the clippy pair; `StaticSnapshotSource`, `ExecEnv::new`, `spawn_*` readers kept) all check out at HEAD (the executor internals now live on in `crates/engine`).

No findings — the commit is clean.

## Verdict
Clean removal-only commit. All deletions were verifiably dead at the commit and remain unreferenced at HEAD (including after the executor code's later move into `crates/engine`); the one touched test was rewired rather than deleted, so behavioural coverage is intact; doc fixes correct genuine drift. No functionality, error handling, or observability was lost.
