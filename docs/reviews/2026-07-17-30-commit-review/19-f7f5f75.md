# 19 f7f5f75 — fix(ci): de-align node_classes to satisfy yamllint colons rule

## Summary of change
Whitespace-only reflow of the `node_classes` map in deploy/cluster/ansible/group_vars/all.yml: the value column was padding-aligned after the colons, tripping yamllint's `colons: max-spaces-after: 1` rule in the cluster-validate job. Each of the six entries is single-spaced; all values (count, ip_start, tier) and the two inline comments are unchanged.

## Findings
None. Verified against the repo's `.yamllint`: the reflowed lines satisfy `colons` (1 space), `braces: max-spaces-inside: 1` (the `{ ... }` flow mappings keep exactly one inside space), and the now single-spaced inline comments are fine because the config relaxes `comments: min-spaces-from-content` to 1. The claim about scripts/check-contract.py holds — it only asserts the literal `node_classes:` key exists (check-contract.py:101), so the contract check is unaffected. The "each with a recorder" inline comment is stale (recorder removed) but pre-dates this commit and is covered by F18.1's docs-drift theme.

## Verdict
Clean, mechanical lint fix; no semantic change, no findings.
