# 23 cef464e — refactor(batcher): remove dead/legacy code and tech debt (#46)

## Summary of change
Dead-code cleanup of `kardamom-batcher`: deletes the entire unused `archive_live.rs` module (live AeronArchive descriptor resolver, 142 lines), the never-called `TypedSegmentReader::collect_all` and `MultiArchiveReader::with_indexes` helpers, the `BatcherError::Aeron` variant whose only constructors lived in the deleted module, and unused Cargo dependencies (`rusteron-archive`, `alloy-consensus`, `alloy-network`, `alloy-rpc-types-eth`; `tokio` demoted to dev-dependencies where tests need it).

## Findings

Dead-code verification: grep at the commit tree and at HEAD found zero references to `archive_live`, `LiveSegmentDescriptor`, `collect_all`, `with_indexes`, or `BatcherError::Aeron` outside the deleted definitions. `tokio` usage in the batcher crate is confined to `tests/` (`#[tokio::test]`), so keeping it only in `[dev-dependencies]` is correct; the batcher binary has a sync main. `with_indexes` was documented as a test seam ("useful for tests that synthesise an in-memory index") but no test ever used it, so nothing was lost. No tests, error handling on live paths, or metrics were removed. The commit message's "kept" list (`kardamom-deployer` build-order dep, `alloy-contract`/`alloy-provider` for `sol!` bindings, `frame.rs` constants, `load_a_index`) checks out — those items remain at HEAD and are referenced.

No findings — the commit is clean.

## Verdict
Clean removal-only commit. Every deleted item was verifiably unreferenced at the commit and remains so at HEAD; dependency pruning matches actual usage (tokio correctly retained for async tests); no functionality, tests, or observability lost. The commit message's kept/removed reasoning is accurate.
